<%= form_with(model: shoppingitem) do |form| %>
  <% if shoppingitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shoppingitem.errors.count, "error") %> prohibited this shoppingitem from being saved:</h2>

      <ul>
        <% shoppingitem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :item_name %>
    <%= form.text_field :item_name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :item_number %>
    <%= form.number_field :item_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
